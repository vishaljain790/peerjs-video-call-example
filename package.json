{
    "name": "peer-messenger-peerserver",
    "version": "0.0.1",
    "scripts": {
        "start": "node website-server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "peer": "^0.2.8"
    }
}
